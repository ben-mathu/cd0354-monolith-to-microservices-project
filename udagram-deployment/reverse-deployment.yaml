apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: reverse
  name: reverse
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reverse
  template:
    metadata:
      labels:
        app: reverse
    spec:
      containers:
      - name: reverse
        image: benatt/reverseproxy:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
